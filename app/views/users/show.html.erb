<table>
  <thead>
    <th><b>image</b></th>
    <th><b>name</b></th>
  </thead>

  <tbody>
    <%  @users.each do |book| %>
    <tr>
      <td><%= user.profile_image_id %></td>
      <td><%= user.name %></td>
      <td><%= link_to 'Show', user_path(user.id) %></td>
    </tr>
   <% end %>
  </tbody>
</table>